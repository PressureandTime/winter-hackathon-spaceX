import styled from 'styled-components';

export const MainLaunchContainer = styled.section`
	display: flex;
	justify-content: center;
    
`;

export const LaunchContainer = styled.div`
	margin: 0.5rem;
	display: flex;
	flex-direction: column;
`;
